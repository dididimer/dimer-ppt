interface:
  display_name: "Dimer PPT"
  short_description: "Image-to-editable-PPT reconstruction with render-diff QA."
  brand_color: "#2563EB"
  default_prompt: "Use $dimer-ppt to convert this image into an editable PPTX, compare the PowerPoint render with the source, and repair any text containment or layout drift."
policy:
  allow_implicit_invocation: true
