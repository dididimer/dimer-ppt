interface:
  display_name: "Dimer PPT Skills"
  short_description: "Image to editable PPT with automatic text layout repair."
  brand_color: "#2563EB"
  default_prompt: "Use $dimer-ppt-skills to convert this image into a truly editable PPTX, compare it with the original, and fix text overlap/layout drift."
policy:
  allow_implicit_invocation: true
